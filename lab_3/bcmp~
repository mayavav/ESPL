#include <stdio.h>
#include <string.h>

//input : 2 files
//output : the position and value of the firste difrent byte
int main(int argc, char **argv) 
{
  	FILE *file = fopen("a.txt", "r");
	char word[100];
	
	if (file == NULL) 
	{
	  printf("file error\n");
	  return 0;
	}
		printf("len of word=%d, word=%s", strlen(word), word);
	bytes_read = fread(word, sizeof(word), 1,file);

}



